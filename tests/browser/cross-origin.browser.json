{
  "schemaVersion": "1.0.0",
  "name": "Mendix origin containment fixture",
  "beforeEach": [{ "action": "goto", "path": "/" }],
  "tests": [
    {
      "name": "interaction navigation cannot leave the configured origin",
      "steps": [
        {
          "action": "click",
          "locator": { "by": "testId", "value": "cross-origin" }
        }
      ]
    }
  ]
}
